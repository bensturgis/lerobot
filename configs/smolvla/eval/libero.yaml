env:
  type: libero
  task: libero_10
  task_ids: [6]

eval:
  batch_size: 1
  n_episodes: 5

# python src/lerobot/scripts/eval.py --config=configs/smolvla/eval/libero.yaml --policy.path=outputs/train/smolvla/libero_filtered_base/checkpoints/120000/pretrained_model