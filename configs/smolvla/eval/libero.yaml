env:
  type: libero
  task: libero_10
  task_ids: [0, 4, 7, 8, 9]
  ood:
    enable: true  

eval:
  batch_size: 3
  n_episodes: 20

seed: 18

# python src/lerobot/scripts/lerobot_eval.py --config=configs/smolvla/eval/libero.yaml --policy.path=outputs/train/smolvla/libero_finetuned_01/checkpoints/015000/pretrained_model